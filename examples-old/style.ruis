use rui::components::*;

// this is a simple stylesheet for rui it just contains the default values for
// the components together with their nesting and possibly with ids
// this Way it is possible to controll everything not explicitely set by the
// Program itsselve. this also includes gradience. Layout however can not be
// affected.

App{
    Button#next{
        Label{color: blue}
    }
    Button#prev{
        Label{color: yellow}
    }
}

App > Button#done{
    Label{color: green}
}
